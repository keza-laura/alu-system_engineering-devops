#!/usr/bin/env bash
# Configures a new Ubuntu machine to have Nginx with a custom HTTP response header
# X-Served-By: <hostname of the server>

# Update and install Nginx
apt-get update -y
apt-get install -y nginx

# Get the hostname of the server
HOSTNAME=$(hostname)

# Remove any existing X-Served-By header config to avoid duplicates
sed -i '/X-Served-By/d' /etc/nginx/sites-available/default

# Add the custom header inside the server block
sed -i "s/server_name _;/server_name _;\n\tadd_header X-Served-By \"$HOSTNAME\";/" /etc/nginx/sites-available/default

# Test config and restart Nginx
nginx -t && systemctl restart nginx

# shellcheck disable=SC2154
